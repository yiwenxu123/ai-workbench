# 优化与 Refactor 路线图（状态）

> 详细工程拆解见 `docs/refactor-checklist.md`。

## 已完成

| 范围 | 内容 |
|------|------|
| Wave 1 | R0 基线、R1 拆 GeneratePanel、R2 model-manifest SSOT |
| 0507 P0 | 构建修复、无 eval、供应商统一、视频链、限流 |
| 0507 P1 | 场景创作 6 卡片、3 步向导、术语推荐、提示词解释区 |
| Sprint 2 | 知识库种子、R3/R5/R6/R7、0507-1/3/4 |
| Sprint 3 | unified-templates、R4 知识收敛、TemplateCenter 拆分 |
| **Sprint 4** | **R8 生成链路闭环**、删除 `modelSizeConfig.ts`、`VideoTemplateWizard` 改读 API、TemplateCenter 二级懒加载、知识 `lastVerified` 过期标记 |

## Refactor 终态（R0–R8）

全部完成。后端 `.env` 配置 Key 后，前端无需再填 API Key 即可生成/编辑（LLM 调优仍需单独配置）。

## 后续可选（P3 / 产品向）

1. 用户反馈与入库审核流
2. 首页纯「任务向导」化（去掉 image/video/edit 三 Tab）
3. TemplateCenter 子 Tab 进一步按路由拆分（当前已 lazy，体积已分散）

## 已知预期限制

- `.env` 中 API Key 为占位时，生成/视频/编辑无法真实调用（需用户自行配置）
- AI 智能调优除知识库外，还需配置 LLM 供应商
