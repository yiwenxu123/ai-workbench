# Refactor 实施审查报告

对照 **《Refactor 任务拆解（可执行版）》**、**《优化计划0507.md》** 以及当前代码库，审查结论如下。

---

## 一、总体结论

**Wave 1（R0 + R1 + R2）已基本完成，质量可接受。**  
自动化验证（后端 32、前端 32、MCP 10）全部通过，与总结一致。

你完成的不仅是 Refactor Wave 1，还覆盖了 **优化计划0507** 中大量 P0/P1/P2 项。两者有重叠，但边界要分清：

| 文档 | 你已完成的大致范围 |
|------|-------------------|
| Refactor Wave 1 | R0、R1、R2 ✅ |
| 优化计划0507 P0/P1 | 构建、eval、供应商、视频链、场景向导、解释区等 ✅ |
| Refactor Wave 2+ | R3–R8 ❌ 未做 |
| 优化计划0507 P1 后半 / P2/P3 | 模板统一、首页减法、内容防过时等 ❌ 部分未做 |

**冒烟结论可信：** 前 5 项无严重对接问题，与代码结构一致。  
**知识库为空** 不是「预期问题」，而是 ** onboarding 缺口**（见下文）。

---

## 二、逐项核实（你声称 vs 代码）

### ✅ 已核实通过

| 项 | 证据 |
|----|------|
| 无 `eval` | 全仓库 grep 无匹配 |
| 生产构建 / CheckmarkCircle | `SaveCaseModal.vue` 使用 `CheckmarkCircle`（ionicons5 正确导出） |
| 视频配置链 | `video.ts` 使用 `getDefaultProviderByCapability('video')` |
| 视频 `task_id` 轮询 | `video.ts` 读取 `result.task_id` 并 `pollTaskResult` |
| 限流 | `main.py` 中 `rate_limit_middleware` |
| 供应商迁移 | `provider.ts` 含 `migrateLegacyConfig` / `migrateLegacyCapabilityProvider` |
| GeneratePanel 拆分 | **147 行**，子目录 `components/generate/` 结构符合 R1 |
| Manifest SSOT | `useModelManifest.ts`、`get_model_manifest()` 含 `name`、视频 4 模型 |
| `modelSizeConfig` 无业务引用 | 仅 `data/modelSizeConfig.ts` 自身 |
| 场景创作 6 卡片 + 3 步 | `SceneQuickEntryModal` + `useSceneQuickEntry` |
| 「为什么这样写」 | `promptExplanation` + Modal UI |
| Manifest `advanced_params` | 后端 dall-e 标记 + 前端优先读 manifest |
| Wave 1 测试数 | 后端 32（含 manifest 3 项），达标 ≥25 |

### ⚠️ 部分完成 / 有保留

| 项 | 说明 |
|----|------|
| **知识推荐** | `useTermSuggestions` 已接 FTS API，但 **空库时无数据**；`TermDictionary` 仍读静态 `terminology.ts`（R4 未做） |
| **AI 智能调优** | 依赖 `knowledge_search`；库空时 LLM 仍可跑，但 **无知识增强素材**（你冒烟已发现） |
| **advanced_params** | 仅 dall-e 在 manifest 标记；`stable-diffusion` 仍靠字符串 fallback，与「完全 Manifest 驱动」有差距 |
| **promptExplanation** | 是 **任务模板文案**，不是知识库/LLM 解释；满足 0507「一句中文解释」，但与 `optimize-prompt` 的 `explanation` 是两套能力 |
| **icons.ts** | 仍映射 `'✅': 'CheckCircle2'`（lucide），与 ionicons 修复 **不一致但不一定触发构建错误** |

### ❌ Refactor 拆解中明确未完成

| 任务 | 现状 |
|------|------|
| **R3** 视频 TaskStatus Adapter | `get_task_status` 仍为 `f"{endpoint}/{task_id}"`，无 `backend/providers/` |
| **R4** 知识数据收敛 | `terminology.ts` / `caseLibrary.ts` 仍被 5+ 处引用 |
| **R5** `build_payload` 集成测试 | 无 `test_build_payload.py` 等 |
| **R6** ConfigModal 拆分 | 仍 **1246 行** |
| **R7** docker-compose | 不存在 |
| **R8** 后端独占 Key 模式 | 未实现 |
| **知识库自动种子** | `init_db()` 只建表；`*.db` 在 `.gitignore`；需手动 `migrate_json_to_sqlite.py` |

---

## 三、Wave 1 完成标准对照

| 标准 | 状态 |
|------|------|
| `GeneratePanel.vue` ≤ 250 行 | ✅ 147 |
| 无业务引用 `modelSizeConfig.ts` | ✅ |
| manifest 含 `name` | ✅ |
| `VideoPanel` 来自 manifest | ✅（有 fallback） |
| `ConfigModal` ≤ 200 行 | ❌ 1246 |
| 视频 status 无硬编码 | ❌ |
| Docker 一键启动 | ❌ |
| 后端测试 ≥ 25 | ✅ 32 |

**Wave 1 核心（R0–R2）：可视为完成。**  
**完整 Refactor 14 项：约完成 3/14（~21%）。**

---

## 四、冒烟问题的根因（知识库为空）

这不是配置问题，而是 **首次启动路径缺失**：

```
backend/data/*.json  (已提交，68 条 knowledge)
        ↓ 需手动执行 migrate_json_to_sqlite.py
backend/data/knowledge.db  (*.db 被 gitignore，clone 后为空)
        ↓ init_db() 只 CREATE TABLE
search_knowledge / optimize-prompt → 0 条
```

**建议优先级 P0（比 R3 更急）：** 启动时若表为空，自动从 JSON 种子导入（幂等）。  
一行命令即可临时修复：

```bash
cd backend && python3 migrate_json_to_sqlite.py
```

---

## 五、优化计划0507 剩余缺口

| 优先级 | 未覆盖项 |
|--------|----------|
| P1 | 首页「任务入口优先」、双侧栏默认隐藏；统一模板中心结构；优化结果固定返回四元组（部分在 PromptOptimizer 有，未端到端统一） |
| P2 | Manifest 驱动 **时长/分辨率** 过滤（视频仍部分用 `shotLanguage.ts` / 静态配置） |
| P2 | MCP 参数完全统一审计（`source_image` 等需全链路 grep 确认） |
| P3 | 内容过时标记、用户反馈、管理员入库流程产品化 |

---

## 六、下一步行动建议（按收益排序）

### 第一优先级（1–2 天，解除冒烟阻塞）

**1. 知识库自动种子（必做）**

- 在 `main.py` startup 或 `knowledge_db.py` 增加 `seed_from_json_if_empty()`
- `scripts/dev.sh` 启动前检测并迁移
- README / `refactor-checklist.md` 写明「首次启动自动灌库」
- 验收：`GET /api/knowledge` 返回 >0；`search_knowledge` 有结果；场景 AI 调优能带上行业术语

**2. 更新 `docs/refactor-checklist.md`**

- 将 Wave 1 四项勾选为完成
- 增加「首次启动知识库 ≥68 条」检查项

### 第二优先级（3–5 天，Wave 2 核心）

**3. R3 — 视频 TaskStatus Adapter**  
即梦/Runway 接入前，轮询逻辑会继续脆弱。

**4. R5 — `build_payload` + 响应解析测试**  
多厂商代理是核心资产，32 个测试大多测知识 API，**生成链路仍无保护网**。

**5. R4 — 知识数据收敛**  
消灭 `TermDictionary` 与 API 双源，与种子数据、AI 入库形成闭环。

### 第三优先级（1 周，可维护性）

**6. R6 — 拆分 ConfigModal（1246 行）**  
当前最大单体组件，是 Wave 1 之后最突出的技术债。

**7. R7 + R8 — Docker + 后端 Key 模式**  
若要给他人用或本机 `.env` 零前端配置，再做。

### 第四优先级（产品向，对齐 0507）

**8. 场景创作端到端闭环**  
场景 → 优化 → 生成 → 一键收录案例（减少 Tab 切换）。

**9. 统一模板中心**  
`dataStore` 六路 API 与 `TemplateCenter` 1100 行可合并瘦身。

**10. `优化建议.md` 整理**  
当前文件内容是 Refactor 拆解全文（430 行），建议拆成：

- `docs/refactor-roadmap.md` — 工程任务（R0–R8）
- `优化建议.md` — 产品/体验向（0507 摘要 + 状态表）

---

## 七、建议的 Sprint 2 排期（可直接执行）

| 天 | 任务 | 产出 |
|----|------|------|
| D1 | 知识库自动种子 + 文档 | 新环境开箱即有知识 |
| D2–D3 | R3 视频 Adapter + 测试 | 多厂商 status 可扩展 |
| D4 | R5 `test_build_payload.py`（5 厂商） | 生成代理有回归网 |
| D5 | R4 前半：`knowledgeStore` + TermDictionary 改 API | 术语单源 |

---

## 八、审查总结

| 维度 | 评价 |
|------|------|
| 完成度（Wave 1） | **高** — R0/R1/R2 达标，测试全绿 |
| 完成度（全 Refactor） | **低** — R3–R8 待做 |
| 与 0507 对齐 | **P0/P1 主体完成**，P2/P3 部分 |
| 冒烟结论 | **可信**；知识库空是 **部署/onboarding bug**，非业务逻辑错误 |
| 最大剩余风险 | ① 空库首次体验 ② 视频多厂商 ③ ConfigModal 单体 ④ 生成链路无测试 |

如果你希望继续落地，建议 **从「知识库自动种子」开始**——改动小、立刻修复你冒烟里唯一的中等严重度问题，并为后续 R4/R5 打基础。需要的话我可以直接实现这一块。


**Sprint 2 第一项已完成：知识库启动自动灌入。**

---

## 做了什么

### 1. 新增 `backend/seed_data.py`
- `seed_from_json_if_empty()`：各表为空时从 `backend/data/*.json` 导入（幂等，不覆盖已有数据）
- `migrate_all_from_json()`：强制 UPSERT，供 CLI 使用

### 2. 启动时自动执行
`main.py` 在 `init_knowledge_db()` 之后调用 `seed_from_json_if_empty()`，clone 后首次 `python3 main.py` 即可有知识数据。

### 3. 简化迁移脚本
`migrate_json_to_sqlite.py` 改为调用 `migrate_all_from_json()`，仅在做**强制全量同步**时需要。

### 4. 测试 + 文档
- 新增 `tests/test_seed_data.py`（3 个用例）→ 后端 **35 passed**
- 更新 `README.md`、`docs/refactor-checklist.md`、`scripts/dev.sh`
- 写入 `优化建议.md` 路线图状态

---

## 你如何验证

```bash
# 模拟全新环境（删除本地库）
rm -f backend/data/knowledge.db

# 启动后端
cd backend && python3 main.py

# 另开终端
curl -s http://127.0.0.1:8000/api/knowledge | python3 -c "import sys,json; d=json.load(sys.stdin); print('knowledge:', len(d))"
curl -s -X POST http://127.0.0.1:8000/api/knowledge/search \
  -H 'Content-Type: application/json' \
  -d '{"query":"电商","scene":"ecommerce","limit":3}'
```

预期：`knowledge` 约 68+ 条，`search` 返回 `total >= 1`。之后 **AI 智能调优** 和 **MCP `search_knowledge`** 应有素材（仍需配置 LLM Key 才能实际调优）。

---

## 下一步建议（Sprint 2 剩余）

| 顺序 | 任务 | 工时 |
|------|------|------|
| 1 | **R3** 视频 TaskStatus Adapter | 2–3 天 |
| 2 | **R5** `test_build_payload.py` | 1–2 天 |
| 3 | **R4** 术语/案例收敛到 API | 3–4 天 |

要继续的话，我可以直接做 **R3 视频轮询适配器**。