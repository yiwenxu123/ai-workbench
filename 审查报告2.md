# Sprint 2 审查报告

对照你列出的任务、Refactor 拆解（R3–R7）和 `优化计划0507.md`，结合当前代码与本地验收结果，结论如下。

---

## 一、总体结论

**Sprint 2 主体可判定为完成，质量达标。**  
你总结里只列了部分任务，但代码库实际还完成了 **R3、R5、R7、知识库自动种子** 等项；后端 **101/101**、构建、MCP **10/10** 均已核实通过。

| 维度 | 评价 |
|------|------|
| 你列出的 6 项 | **5 项完全达标，1 项部分达标** |
| Refactor Wave 2（R3–R7） | **约 5/6 完成**（缺 R4、R8） |
| 0507 产品向 | **P0/P1 主体完成**；模板中心 UI 层、首页彻底减法仍未到位 |

---

## 二、逐项核实（你声称 vs 代码）

### ✅ 完全通过

| 任务 | 核实 |
|------|------|
| **R6 ConfigModal ≤200 行** | `ConfigModal.vue` **157 行** ✓ |
| **0507-1 面板默认隐藏** | `panelOpen` 首次为 `false`（`App.vue:168`）✓ |
| **0507-1 引导条移除** | 无 `showPanelGuide` / `panel-guide-bar` ✓ |
| **0507-3 时长/分辨率 Manifest** | `VideoPanel` 从 `getModel()` 读 `durations`/`resolutions`，切换模型时校正 store ✓ |
| **0507-4 MCP 参数审计** | `mcp-server/test.js` 断言 `source_image`、无 `image_url`；**10 passed** ✓ |
| **构建 + pytest** | `vite build` 成功；`pytest` **101 passed** ✓ |

### ⚠️ 部分通过 / 表述需修正

| 任务 | 实际情况 |
|------|----------|
| **0507-2 统一模板中心** | **数据层未真正统一**。`dataStore.loadAll()` 仍并行请求 **6 个** API；`apiService.getUnifiedTemplates()` 已实现但 **无任何组件调用**；`TemplateCenter.vue` 仍 **1100 行**，继续读 `workTemplates`/`videoTemplates`/`festivalTemplates` 等分字段 |
| **R6「拆分」** | 壳层达标，但配置总代码 **~1416 行**（`config/` 下 8 个子组件），债务从单文件 **转移** 到子目录，未消减 |
| **验收 101/101** | 准确指 **后端 pytest**；前端 vitest 仍为 **32**，应分开表述 |

### ✅ 你未列出但 Sprint 2 已完成的项

| 任务 | 证据 |
|------|------|
| **R3 视频 Adapter** | `backend/providers/`（kling/jimeng/runway）；`get_task_status` 走 `get_adapter()`；`video.ts` 轮询传 `provider` |
| **R5 build_payload 测试** | `test_build_payload.py` **43** 个用例 |
| **R7 docker-compose** | 根目录 `docker-compose.yml` + 前后端 Dockerfile |
| **知识库自动种子** | `seed_data.py` + startup 调用；`test_seed_data.py` |
| **Provider 测试** | `test_providers.py` **23** 用例 |

---

## 三、Refactor 全量完成度（更新）

| 任务 | 状态 | 备注 |
|------|------|------|
| R0–R2 | ✅ | Wave 1 |
| R3 | ✅ | Adapter 工厂 + 前端传 provider |
| R4 | ❌ | `terminology.ts` / `caseLibrary.ts` 仍 5+ 处引用；`TermDictionary` 未改 API |
| R5 | ✅ | 43 个 payload/校验测试 |
| R6 | ✅ | 壳 157 行；子组件未再拆 |
| R7 | ✅ | docker-compose 存在 |
| R8 | ❌ | 无「仅后端 .env Key、前端隐藏输入」模式 |
| 知识库种子 | ✅ | 已修复冒烟「空库」问题 |

**Refactor 14 项：约 11/14（~79%）完成。**

---

## 四、0507 计划对齐

| 块 | 状态 |
|----|------|
| P0 稳定性 | ✅ 基本完成 |
| P1 小白路径 | ✅ 场景向导、解释区、参数折叠；⚠️ 首屏仍是三 Tab，非「纯任务首页」 |
| P1 模板体系 | ⚠️ 后端有 `unified-templates`，**前端消费层未切换** |
| P2 Manifest | ✅ 图像尺寸 + 视频时长/分辨率 |
| P2 Agent/MCP | ✅ 工具/资源/参数测试齐全 |
| P3 内容防过时 | ❌ 未做 |

---

## 五、仍值得关注的问题

### 1. 文档与代码不同步（低，但易误导）

- `docs/refactor-checklist.md` 的 Wave 2 仍标 R3–R7 待办 → 应更新为已完成
- `优化建议.md` 仍写 Sprint 2 进行中 → 应刷新状态表

### 2. 0507-2 名实不符（中）

`/api/unified-templates` 是 dead API path（仅 `api/index.ts` 定义）。模板仍多源维护，与「统一模板中心」目标有差距。

### 3. R4 知识双源（中）

`useTermSuggestions` 已走 FTS，但 `TermDictionary` / `CaseLibraryPanel` 仍依赖静态文件；AI 入库内容不会自动出现在词典 UI。

### 4. 构建体积（低）

`TemplateCenter` chunk **~609KB**（gzip 156KB），首屏懒加载可接受，但模板中心仍是性能热点。

### 5. R8 与真实 Key（预期）

占位 Key 下生成失败仍属预期；若要做「配好 `.env` 零前端配置」，R8 未做。

---

## 六、对你总结表的裁定

| # | 你的结论 | 审查裁定 |
|---|----------|----------|
| R6 402→157 | ✅ | **通过** |
| 0507-1 面板隐藏 | ✅ | **通过** |
| 0507-2 数据层统一 | ✅ | **部分通过** — load 仍 6 路，unified API 未接入 |
| 0507-3 Manifest 过滤 | ✅ | **通过** |
| 0507-4 MCP 审计 | ✅ | **通过** |
| 101/101 + build | ✅ | **通过**（注明后端 101 + 前端 32） |

---

## 七、下一步建议（Sprint 3）

按收益排序：

1. **接通 `unified-templates`**（半天）  
   `dataStore` 改单次 `getUnifiedTemplates()`，TemplateCenter 按 `type`/`taskType` 分组 —— 真正闭合 0507-2。

2. **R4 知识收敛**（2–3 天）  
   `TermDictionary` / `CaseLibraryPanel` 改读 API；删除静态 `terminology` 数组引用。

3. **更新文档**（0.5 天）  
   同步 `refactor-checklist.md`、`优化建议.md`、`审查结果.md`。

4. **R8 后端 Key 模式**（1 天）  
   `has_backend_config` 时隐藏前端 Key 输入、请求不传 `api_key`。

5. **TemplateCenter 瘦身**（可选，2 天）  
   1100 行 + 609KB chunk，可拆「我的模板 / 官方 / 节庆 / 视频」子面板。

6. **0507 P3**（中长期）  
   内容过时标记、使用反馈、入库审核流。

---

## 八、一句话总结

**Sprint 2 工程质量可信，可合并/发版；你列的任务基本属实，但 0507-2「统一模板」应标为部分完成。**  
Refactor 主链路（R3/R5/R6/R7 + 知识种子）已在库中，建议 Sprint 3 优先 **unified-templates 真正接入** 和 **R4 消双源**，并刷新文档避免后续审查误判。

如需，我可以直接提交：**更新三份文档** + **`dataStore` 切换 unified-templates** 的最小 PR 方案。