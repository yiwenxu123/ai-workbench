# AI绘图工作台全面优化计划

## Summary
- 目标：把项目从“功能堆叠型 AI 绘图工具”升级为“面向小白的 AI 图像/视频创作学习工作台”，同时保留可被 OpenClaw、Hermes、Claude、Codex 等 Agent 调用的标准能力层。
- 优先级：小白体验第一，技术稳定第二，Agent 生态第三。
- 部署默认：本地个人版，不引入复杂账号/计费/SaaS 运维；但为后续私有团队版预留清晰边界。
- 核心策略：前台做减法，后台做标准化；用户看到“任务向导”，Agent 看到“稳定工具”。

## Key Changes

### P0：稳定性与可用性修复
- 修复当前生产构建失败：移除未使用的 `NSelect`、`llmModelOptions`、`getCasesByCategory`。
- 替换本地数据抽取脚本中的 `eval`，改为安全的静态解析或明确 JSON 数据源。
- 统一图像、视频、编辑、视觉、LLM 的供应商配置模型，废弃视频/编辑独立散落的 `localStorage` Key。
- 修复视频生成配置链路：视频生成必须读取视频供应商配置，而不是默认图像供应商。
- 补齐视频异步任务轮询：提交任务后进入“排队/生成中/成功/失败”状态，不再假设接口立即返回视频 URL。
- 对 `/generate`、`/generate-video`、`/edit-image`、`/api/ingest/*` 全部加统一限流与错误格式。
- 验收：`npm run build`、`npm run test:run`、后端语法检查全部通过；图像、视频、编辑配置互不串线。

### P1：小白主路径重构
- 首页改为“我想做什么”任务入口，而不是默认暴露左右双侧栏和大量工具 Tab。
- 首屏固定 6 个任务卡片：电商主图、社媒海报、PPT 配图、头像/形象照、图生视频、图片修改。
- 每个任务进入 3 步向导：选择场景 → 填一句人话需求 → 生成/优化/保存。
- 默认隐藏 Seed、步数、CFG、采样器等专家参数，放入“高级设置”；新手模式不展示技术术语。
- 把知识库改成上下文提示：用户输入时自动推荐术语、镜头语言、负面词和优秀案例，不要求用户先阅读资料。
- 增加“为什么这样写提示词”解释区，用一句中文解释模型需要的主体、风格、构图、光线、用途。
- 验收：新用户不看文档也能在 3 分钟内完成一次生图；从生图结果一键进入编辑或图生视频。

### P1：提示词与模板体系升级
- 建立统一的任务模板结构：`taskType`、`audience`、`requiredFields`、`promptTemplate`、`negativePrompt`、`recommendedModels`、`examples`。
- 将现有提示词库、节庆模板、视频模板、行业知识映射到统一模板中心。
- 增加“中文需求 → 专业提示词”的默认优化流程，普通用户只填中文需求。
- 提示词优化结果必须返回：优化后提示词、负面词、可读解释、可继续修改的问题。
- 验收：所有模板都能被 Web 向导、批量生成、Agent 工具复用，避免重复维护。

### P2：模型能力注册表
- 新增模型能力 Manifest，由后端提供，前端不再硬编码模型价格、能力、尺寸。
- Manifest 最小字段：模型 ID、供应商、能力类型、支持尺寸/比例、是否异步、推荐场景、限制说明、更新时间。
- 前端根据 Manifest 自动过滤尺寸、时长、分辨率和入口能力。
- 模型过期不改 UI 代码，只更新 Manifest。
- 验收：新增一个模型时，不需要改 3 个以上业务组件；不支持的视频/尺寸组合不会出现在 UI 中。

### P2：Agent 能力层
- 保留 OpenAPI Skills，同时把 MCP Server 改成标准稳定工具层。
- MCP 工具统一为：`generate_image`、`generate_video`、`check_task_status`、`edit_image`、`list_templates`、`get_template`、`list_cases`、`extract_knowledge`。
- MCP 参数与后端接口完全一致，例如统一使用 `source_image`，不再出现 `image_url` 与 `source_image` 混用。
- MCP Server 的 API Base 支持环境变量配置，不再硬编码 `127.0.0.1:8000`。
- 增加 MCP Resources：模板库、案例库、模型能力 Manifest、术语知识库。
- 增加 MCP Prompts：小白提示词改写、图生视频脚本生成、图片编辑指令生成。
- 验收：Hermes/OpenClaw/Claude/Codex 通过 MCP 能列工具、读模板、调用生图/生视频、查询任务状态。

### P3：内容更新与防过时机制
- AI 入库从“高级工具 Tab”移到“内容维护/管理员模式”，默认对小白隐藏。
- 入库流程增加来源、更新时间、标签、适用模型、质量评分、人工确认状态。
- 每条模板/案例记录最后验证时间，超过阈值标记“可能过时”。
- 增加“用户反馈”：好用/不好用、生成失败原因、模板使用次数。
- 建立每月内容更新流程：抓取资料 → AI 提取 → 去重对比 → 人工确认 → 发布到模板中心。
- 验收：项目不依赖一次性写死资料；热门模型和创作玩法变化时，可以通过内容库更新追上。

## Public Interfaces / Types
- 新增统一供应商类型：同一个 Provider 支持 `image`、`video`、`edit`、`vision`、`llm` 多能力声明。
- 新增任务状态接口规范：所有异步生成统一返回 `task_id`、`status`、`progress`、`result_url`、`error`。
- 新增模型 Manifest 接口：前端和 MCP 都从同一份能力注册表读取模型能力。
- MCP 工具命名统一为 snake_case，参数与后端 Pydantic 请求模型保持一致。
- OpenAPI Skills 只暴露 Agent 必要能力，不暴露内容保存、管理、密钥配置等内部接口。

## Test Plan
- 构建测试：`npm run build`、`npm run typecheck`、`npm run test:run` 必须通过。
- 后端测试：校验生图、生视频、任务查询、图片编辑、入库提取的成功/失败响应格式一致。
- 配置测试：分别配置图像、视频、编辑、视觉、LLM 供应商，确认不会互相覆盖或误用。
- 新手路径测试：无 API 配置时能明确引导；配置后能完成任务卡片 → 生成 → 保存/编辑/转视频。
- 视频异步测试：模拟 pending、processing、succeed、failed，前端状态展示和重试逻辑正确。
- MCP 测试：本地 MCP Client 能列出工具、读取资源、调用生图/生视频并查询任务。
- 回归测试：已有模板、历史记录、作品库、提示词优化、图片分析功能保持可用。

## Assumptions
- 默认按“本地个人版”优化，不做公开 SaaS 的账号、计费、内容审核和多租户权限。
- 默认先做“小白模式”，专家参数保留但不作为主入口。
- 不深度绑定 OpenClaw 或 Hermes；统一支持 MCP/OpenAPI，避免被单一 Agent 平台锁定。
- 不一次性重写全部 UI；采用渐进式改造：先修底座，再重构首页和任务向导，再扩展 Agent 能力。
- 工程原则：KISS 优先减少入口复杂度；YAGNI 不做 SaaS 级能力；DRY 统一模板和供应商模型；SOLID 将 UI 向导、供应商能力、生成任务、Agent 工具分层解耦。
